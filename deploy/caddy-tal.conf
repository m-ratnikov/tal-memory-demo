# Add this block to the VPS Caddyfile, then reload Caddy.
# No `import auth`: the app has its own DEMO_PASSWORD gate, so the demo gets a
# DEDICATED password (shareable with the client) instead of your shared basic-auth.
tal.softwisery.com {
	reverse_proxy host.docker.internal:8010
}
